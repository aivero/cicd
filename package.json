{
  "license": "MIT",
  "scripts": {
    "start": "deno run --unstable --allow-all --import-map ./import_map.json ./lib/es6_global/src/generate_config.js",
    "build": "rescript build",
    "watch": "rescript build -w",
    "clean": "rescript clean"
  },
  "devDependencies": {
    "rescript": "^9.1.4"
  },
  "dependencies": {
    "@ryyppy/rescript-promise": "^2.1.0"
  }
}
