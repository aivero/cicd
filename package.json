{
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "deno run --unstable --allow-all --import-map ./import_map.json ./lib/es6/src/GenerateConfig.js",
    "build": "rescript build -with-deps",
    "dev": "rescript build -w -with-deps",
    "clean": "rescript clean"
  },
  "devDependencies": {
    "rescript": "^9.1.4"
  },
  "dependencies": {
    "@prefix_re/std": "^0.0.10"
  }
}
