// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Git from "./Git.js";
import * as Job from "../job/Job.js";
import * as Seq from "../std/Seq.js";
import * as Flat from "../std/Flat.js";
import * as Proc from "../std/Proc.js";
import * as Task from "../std/Task.js";
import * as Config from "../Config.js";
import * as Manual from "./Manual.js";
import * as Instance from "../Instance.js";
import * as Belt_Array from "../../../../node_modules/rescript/lib/es6/belt_Array.js";
import * as TaskResult from "../std/TaskResult.js";
import * as Belt_Result from "../../../../node_modules/rescript/lib/es6/belt_Result.js";

function findAllInts(recursive) {
  var cmd = [
      "git",
      "ls-files",
      "**devops.yml"
    ].concat(recursive ? ["--recurse-submodules"] : []);
  return TaskResult.flatMap(Proc.run(cmd), (function (e) {
                return Belt_Result.map(Seq.result(Belt_Array.map(e.trim().split("\n"), Config.loadFile)), Flat.array);
              }));
}

function findReqs(ints, allInts) {
  return Task.map(Promise.all([
                  ints,
                  allInts
                ]), (function (param) {
                var allInts = param[1];
                var ints = param[0];
                if (ints.TAG !== /* Ok */0) {
                  return {
                          TAG: /* Error */1,
                          _0: ints._0
                        };
                }
                var ints$1 = ints._0;
                if (allInts.TAG !== /* Ok */0) {
                  return {
                          TAG: /* Error */1,
                          _0: allInts._0
                        };
                }
                var reqs = Flat.array(Belt_Array.map(ints$1, (function ($$int) {
                            var req = $$int.req;
                            if (req !== undefined) {
                              return req;
                            } else {
                              return [];
                            }
                          })));
                var reqs$1 = allInts._0.filter(function ($$int) {
                      var name = $$int.name;
                      if (name !== undefined ? reqs.includes(name) : false) {
                        return !Belt_Array.some(ints$1, (function ($$int) {
                                      var name = $$int.name;
                                      if (name !== undefined) {
                                        return reqs.includes(name);
                                      } else {
                                        return false;
                                      }
                                    }));
                      } else {
                        return false;
                      }
                    });
                return {
                        TAG: /* Ok */0,
                        _0: Belt_Array.concat(ints$1, reqs$1)
                      };
              }));
}

function load(param) {
  var kind = Deno.env.get("mode");
  var recursive = Deno.env.get("recursive");
  var source = Deno.env.get("CI_PIPELINE_SOURCE");
  var recursive$1;
  if (recursive == null) {
    recursive$1 = true;
  } else {
    switch (recursive) {
      case "0" :
      case "false" :
          recursive$1 = false;
          break;
      case "1" :
      case "true" :
          recursive$1 = true;
          break;
      default:
        recursive$1 = true;
    }
  }
  var allInts = findAllInts(recursive$1);
  var ints;
  if (kind == null) {
    ints = (source == null) || source !== "web" ? Git.findInts(undefined) : Manual.findInts(allInts);
  } else {
    switch (kind) {
      case "git" :
          ints = Git.findInts(undefined);
          break;
      case "manual" :
          ints = Manual.findInts(allInts);
          break;
      default:
        ints = Task.resolve({
              TAG: /* Error */1,
              _0: "Mode not supported: " + kind
            });
    }
  }
  var ints$1 = findReqs(ints, allInts);
  return TaskResult.flatten(TaskResult.flatMap(ints$1, (function (ints) {
                    return Job.load(Instance.zip(ints));
                  })));
}

export {
  findAllInts ,
  findReqs ,
  load ,
  
}
/* Git Not a pure module */
